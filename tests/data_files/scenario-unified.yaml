# Configuration file that sets the helm chart into unified (legacy) mode.

global:
  plane:
    mode: unified

  postgresql:
    enabled: true

tags:
  logging: true
  monitoring: true
  platform: true

astronomer:
  houston:
    upgradeDeployments:
      enabled: true
