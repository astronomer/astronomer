# Helm values for control scenario tests.

global:
  plane:
    mode: control

  # This usage is deprecated, use `global.plane.mode` instead.
  controlplane:
    enabled: true

  postgresqlEnabled: true

tags:
  # logging: true
  # monitoring: true
  platform: true

astronomer:
  houston:
    upgradeDeployments:
      enabled: true
