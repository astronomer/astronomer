{{- if .Values.global.chartMuseumEnabled }}
apiVersion: v1
kind: Service
metadata:
  name: chartmuseum
spec:
  type: ClusterIP
  selector:
    app: chartmuseum
  ports:
  - port: 8080
    targetPort: 8080
{{- end }}