apiVersion: v1
data:
  index_template.json: |-
    {
      "template": {
        "settings": {},
        "mappings": {
          "properties": {
            "date_nano": {
              "type": "date_nanos"
            }
          }
        },
        "aliases": {}
      }
    }
kind: ConfigMap
metadata:
  name: es-template
  namespace: kube-system
