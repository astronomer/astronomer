apiVersion: v1
data:
  index_template.json: |-
    {
      "index_patterns": [
          "logstash-default*"
      ],
      "mappings": {
        "properties": {
          "date_nano": {
            "type": "date_nanos"
          }
        }
      }
    }
kind: ConfigMap
metadata:
  name: es-template
  namespace: kube-system
