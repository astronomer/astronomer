################################
## Prometheus NetworkPolicy   ##
################################
{{- if .Values.global.features.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ template "prometheus.fullname" . }}-policy
  labels:
    tier: monitoring
    component: {{ template "prometheus.name" . }}
    chart: {{ template "prometheus.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  podSelector:
    matchLabels:
      tier: monitoring
      component: {{ template "prometheus.name" . }}
      release: {{ .Release.Name }}
  policyTypes:
  - Ingress
  ingress:
  - from:
    {{- if or (eq .Values.global.plane.mode "unified") (eq .Values.global.plane.mode "control") }}
    - podSelector:
        matchLabels:
          tier: astronomer
          component: houston
          release: {{ .Release.Name }}
    {{- end }}
    {{- if (eq .Values.global.plane.mode "data") }}
    - podSelector:
        matchLabels:
          tier: monitoring
          component: {{ template "prometheus.name" . }}-federation-auth
          release: {{ .Release.Name }}
    {{- end }}
    - podSelector:
        matchLabels:
          tier: monitoring
          component: grafana
          release: {{ .Release.Name }}
    {{- if .Values.global.pgbouncer.enabled }}
    - podSelector:
        matchLabels:
          tier: astronomer
          component: pgbouncer
          release: {{ .Release.Name }}
    {{- end }}
    {{- if .Values.global.features.authSidecar.enabled  }}
    - namespaceSelector:
        matchLabels:
          network.openshift.io/policy-group: ingress
    {{- if .Values.global.features.authSidecar.ingressAllowedNamespaces }}
    - namespaceSelector:
        matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: In
          values: {{ .Values.global.features.authSidecar.ingressAllowedNamespaces | toJson }}
    {{- end }}
    {{- else }}
    - podSelector:
        matchLabels:
          tier: nginx
          component: {{ ternary "dp-ingress-controller" "cp-ingress-controller" (eq .Values.global.plane.mode "data") }}
          release: {{ .Release.Name }}
    {{- end }}
    # It is sometimes useful to whitelist other pods,
    # for example admin Airflow tasks.
    {{- range .Values.ingressNetworkPolicyExtraSelectors }}
    -
    {{- toYaml . | nindent 6 }}
    {{- end }}
    ports:
    - protocol: TCP
      port: {{ .Values.ports.http }}
    {{- if .Values.global.features.authSidecar.enabled  }}
    - protocol: TCP
      port: {{ .Values.global.features.authSidecar.port }}
    {{- end }}
{{- end }}
