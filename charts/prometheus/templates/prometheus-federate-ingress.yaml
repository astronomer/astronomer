########################################
## Prometheus Federate Ingress ##
########################################
{{- if eq (include "global.ingress.enabled" .) "true" }}
{{- if eq .Values.global.plane.mode "data" }}
kind: Ingress
apiVersion: {{ template "apiVersion.Ingress" . }}
metadata:
  name: {{ template "prometheus.fullname" . }}-federate-ingress
  labels:
    tier: prometheus-networking
    component: prometheus-federate-ingress
    chart: {{ template "prometheus.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    {{- if .Values.global.authSidecar.enabled }}
    {{- toYaml .Values.global.extraAnnotations | nindent 4 }}
    {{- else }}
    {{- if not .Values.global.ingressClassName }}
    kubernetes.io/ingress.class: "{{ .Release.Name }}-nginx"
    {{- end }}
    {{- if .Values.global.extraAnnotations }}
    {{- toYaml .Values.global.extraAnnotations | nindent 4 }}
    {{- end }}
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"
    {{- end }}
    {{- if .Values.federation.ingress.annotations }}
    {{- toYaml .Values.federation.ingress.annotations | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ include "global.ingressClassName" . }}
  {{- if .Values.global.tlsSecret }}
  tls:
    - secretName: {{ .Values.global.tlsSecret }}
      hosts:
        - {{ template "prom-proxy.url" . }}
  {{- end }}
  rules:
    - host: {{ template "prom-proxy.url" . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ template "prometheus.fullname" . }}-prom-proxy
                port:
                  name: http
{{- end }}
{{- end }}
