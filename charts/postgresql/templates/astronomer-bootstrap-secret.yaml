#################################
## Astronomer Bootstrap Secret
#################################
{{- if .Values.global.postgresqlEnabled }}
apiVersion: v1
kind: Secret
metadata:
  name: astronomer-bootstrap
data:
  connection:  {{ include "pgbouncer_users" . | b64enc | quote }}
{{- end }}
