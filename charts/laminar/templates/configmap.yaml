#####################
## laminar.env file ##
#####################
{{- if eq (include "laminar.enabled" .) "true" }}
{{- if and .Values.envConfigMap.enabled .Values.envConfigMap.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.envConfigMap.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "laminar.labels" (list $ "laminar") | nindent 4 }}
data:
  laminar.env: |
    harmony_enable_healers: False
    harmony_enable_health_incidents: False
    harmony_configmap_metrics_enabled: False
    harmony_configmap_metrics_use_informer: False
{{ .Values.envConfigMap.content | indent 4 }}
{{- end }}
{{- end }}
