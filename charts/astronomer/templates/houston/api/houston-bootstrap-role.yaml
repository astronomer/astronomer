############################
## Houston Bootstrap Role ##
############################
{{- if and .Values.global.rbacEnabled (and (not .Values.houston.backendSecretName) (not .Values.houston.backendConnection)) }}
kind: Role
apiVersion: {{ template "apiVersion.rbac" . }}
metadata:
  name: {{ .Release.Name }}-houston-bootstrapper
  {{- if not $.Values.global.disableManageClusterScopedResources }}
  labels:
    tier: houston
    release: {{ .Release.Name }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: {{ .Release.Service }}
  annotations:
  {{- if not $.Values.global.features.namespacePools.namespaces.create }}
    helm.sh/resource-policy: keep
  {{- end }}
  {{ if .Values.global.enableArgoCDAnnotation }}
    argocd.argoproj.io/sync-wave: "-1"
  {{- end }}
  {{- end }}
rules:
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - list
      - get
      - create
      - patch
{{- end }}
