################################
## MCP Server NetworkPolicy
################################
{{- if .Values.global.networkPolicy.enabled }}
{{- if eq (include "mcpServer.enabled" .) "true" }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-mcp-server-policy
  labels:
    tier: astronomer
    component: mcp-server-policy
    release: {{ .Release.Name }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: {{ .Release.Service }}
    plane: {{ .Values.global.plane.mode }}
  {{ if .Values.global.argoCD.annotation.enabled }}
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  {{- end }}
spec:
  podSelector:
    matchLabels:
      tier: astronomer
      component: {{ template "mcpServer.name" . }}
      release: {{ .Release.Name }}
  policyTypes:
  - Ingress
  ingress:
  - from:
    # BYO-ingress is unsupported with the MCP server (see mcp-server-ingress.yaml), so the
    # only reachable peer here is the in-cluster nginx controller.
    - podSelector:
        matchLabels:
          tier: nginx
          component: {{ ternary "dp-ingress-controller" "cp-ingress-controller" (eq .Values.global.plane.mode "data")}}
          release: {{ .Release.Name }}
    ports:
    - protocol: TCP
      port: {{ .Values.ports.mcpServerHTTP }}
{{- end }}
{{- end }}
