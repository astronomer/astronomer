# Namespace Pools - create user-defined namespaces to host Airflow deployments
{{- if and .Values.global.namespaceManagement.namespacePools.enabled .Values.global.namespaceManagement.namespacePools.namespaces.create }}
{{- $namespaceLabels := .Values.global.namespaceLabels }}
{{- range $i, $namespaceName := .Values.global.namespaceManagement.namespacePools.namespaces.names }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ $namespaceName }}
  annotations:
    # Keep namespaces if user disable namespace pools later on
    "helm.sh/resource-policy": keep
  {{- if $namespaceLabels }}
  labels:
    {{- $namespaceLabels | toYaml | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
