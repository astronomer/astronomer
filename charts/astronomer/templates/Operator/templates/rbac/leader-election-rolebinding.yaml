apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: airflow-operator-leader-election-rolebinding
  namespace: '{{ .Release.Namespace }}'
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: airflow-operator-leader-election-role
subjects:
- kind: ServiceAccount
  name: airflow-operator-controller-manager
  namespace: '{{ .Release.Namespace }}'
