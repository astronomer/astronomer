################################
## Astronomer Phoenix Secret
#################################
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-jwt-secret
  labels:
    tier: astronomer
    release: {{ .Release.Name }}
    chart: {{ .Chart.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  secret: {{ .Values.jwtSecret | b64enc }}
