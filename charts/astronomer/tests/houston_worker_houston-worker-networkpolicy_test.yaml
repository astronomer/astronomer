### DEPRECATED
#
#   helm-unittest usage is deprecated in this repository.
#   Do not modify these tests. If changes are needed,
#   migrate the test to the pytest test suite.
#
##############

suite: Test houston/worker/houston-worker-networkpolicy.yaml
templates:
- houston/worker/houston-worker-networkpolicy.yaml
tests:
- it: should work
  asserts:
    - isKind:
        of: NetworkPolicy
