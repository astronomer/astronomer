### DEPRECATED
#
#   helm-unittest usage is deprecated in this repository.
#   Do not modify these tests. If changes are needed,
#   migrate the test to the pytest test suite.
#
##############

suite: Test houston/cronjobs/houston-cleanup-deployments-cronjob.yaml
templates:
- houston/cronjobs/houston-cleanup-deployments-cronjob.yaml
tests:
- it: should work
  asserts:
    - isKind:
        of: CronJob
