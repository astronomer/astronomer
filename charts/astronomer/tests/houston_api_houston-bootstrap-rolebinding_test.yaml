### DEPRECATED
#
#   helm-unittest usage is deprecated in this repository.
#   Do not modify these tests. If changes are needed,
#   migrate the test to the pytest test suite.
#
##############

suite: Test houston/api/houston-bootstrap-rolebinding.yaml
templates:
- houston/api/houston-bootstrap-rolebinding.yaml
tests:
- it: should work
  asserts:
    - isKind:
        of: RoleBinding
