### DEPRECATED
#
#   helm-unittest usage is deprecated in this repository.
#   Do not modify these tests. If changes are needed,
#   migrate the test to the pytest test suite.
#
##############

suite: Test houston/api/houston-jwt-certificate-secret.yaml
templates:
- houston/api/houston-jwt-certificate-secret.yaml
tests:
- it: should work
  asserts:
    - isKind:
        of: Secret
