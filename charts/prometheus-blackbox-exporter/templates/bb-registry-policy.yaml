apiVersion: extensions/v1beta1
kind: NetworkPolicy
metadata:
  labels:
    release: {{ .Release.Name}}
    tier: monitoring
  name: bb-registry-policy
  namespace: astronomer
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          component: blackbox
          release: astronomer
          tier: monitoring
    ports:
    - port: 5000 
      protocol: TCP
  podSelector:
    matchLabels:
      component: registry
      release: astronomer
      tier: astronomer
  policyTypes:
  - Ingress
