# TODO how do we easily handle not applying this when not in cloud
{{- if gt (.Capabilities.KubeVersion.Minor | atoi) 15 }}
apiVersion: networking.k8s.io/v1
{{- else }}
apiVersion: extensions/v1beta1
{{- end }}
# apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  labels:
    release: {{ .Release.Name}}
    tier: monitoring
  name: bb-houston-policy
  namespace: astronomer
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          component: blackbox-exporter
          release: astronomer
          tier: monitoring
    ports:
    - port: 8871
      protocol: TCP
  podSelector:
    matchLabels:
      component: houston
      release: astronomer
      tier: astronomer
  policyTypes:
  - Ingress