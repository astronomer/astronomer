---
suite: Test deployment
templates:
  - deployment.yaml
tests:
  - it: should work
    set:
      global:
        blackboxExporterEnabled: true
    asserts:
      - isKind:
          of: Deployment
