### DEPRECATED
#
#   helm-unittest usage is deprecated in this repository.
#   Do not modify these tests. If changes are needed,
#   migrate the test to the pytest test suite.
#
##############

---
suite: Test grafana-bootstrap-serviceaccount
templates:
  - grafana-bootstrap-serviceaccount.yaml
tests:
  - it: should work
    asserts:
      - isKind:
          of: ServiceAccount
