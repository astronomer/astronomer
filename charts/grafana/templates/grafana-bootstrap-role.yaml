############################
## Grafana Bootstrap Role ##
############################
{{- if eq (include "astronomer.controlPlaneEnabled" .) "true" }}
{{- if and .Values.global.rbac.enabled (and (not .Values.backendSecretName) (not .Values.backendConnection)) }}
kind: Role
apiVersion: {{ template "apiVersion.rbac" . }}
metadata:
  name: {{ template "grafana.fullname" . }}-bootstrapper
  labels:
    tier: grafana
    chart: {{ template "grafana.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
rules:
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - list
      - get
      - create
      - patch
{{- end }}
{{- end }}
