{{ define "grafana_image" -}}
{{ .Values.image }}:{{ or .Values.global.imageTag .Values.imageTag }}
{{- end }}

{{ define "grafana_url" -}}
{{ .Release.Name }}-grafana.{{ .Values.global.baseDomain }}
{{- end }}
