#!/usr/bin/env sh

set -e

# Substitue env vars
envsubst '$APP_API_LOC_HTTPS $APP_API_LOC_WSS $ANALYTICS_TRACKING_ID $STRIPE_PK $APP_ENABLE_ERROR_REPORTING $APP_SENTRY_DSN $APP_LOGROCKET' < /etc/nginx/nginx.conf.tpl > /etc/nginx/nginx.conf

# Run the original command
exec nginx -g 'daemon off;'
