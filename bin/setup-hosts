#!/usr/bin/env bash
# This contents of this file must be compatible with CI and local dev workflows
set -euo pipefail

grep -q "local.astronomer-development.com" /etc/hosts || {
  echo "172.17.0.1 local.astronomer-development.com" |
  sudo tee -a /etc/hosts
}
