#!/bin/sh

for component in ${COMPONENTS} ; do
    echo "Building ${component}..."

    DOCKER_FILE="images/clickstream/${component}/Dockerfile"
    COMPONENT_NAME=${component}

    docker build -t ${REPOSITORY}/${COMPONENT_NAME}:latest -f ${DOCKER_FILE} images/clickstream/${component} || exit 1
done
